extends layout

block content
  h1=title
  p Welcome to #{title}
  h3 UserList(size=#{unList.length}):

  span#example.block

block scripts
  script(src='/static/javascripts/index.jsx', type='text/jsx')
